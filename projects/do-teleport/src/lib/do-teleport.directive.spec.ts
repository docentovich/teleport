import { DoTeleportDirective } from './do-teleport.directive';

describe('TeleportDirective', () => {
  it('should create an instance', () => {
    const directive = new DoTeleportDirective();
    expect(directive).toBeTruthy();
  });
});
